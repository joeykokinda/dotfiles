general {
    # lock screen with hyprlock after 10 min
    lock_cmd = hyprlock
}

listener {
    timeout = 1500            
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

# suspend after 1 hour
listener {
    timeout = 3600
    on-timeout = systemctl suspend
}

